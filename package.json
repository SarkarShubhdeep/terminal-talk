{
  "name": "terminal-talk",
  "module": "src/server.ts",
  "type": "module",
  "version": "1.0.0",
  "private": false,
  "bin": {
    "terminal-talk": "dist/client.js"
  },
  "scripts": {
    "build": "bun build src/client.ts --target node --outdir dist",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.18.1",
    "bun-types": "^1.3.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
